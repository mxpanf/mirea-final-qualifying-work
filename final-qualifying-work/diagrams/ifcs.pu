@startuml ifcs
start
:Получение данных от студента;
if (Данные корректны?) then (да)
  :Создание запроса на выпуск;
  :Отправка запроса комиссии;
  if (Подписи собраны?) then (да)
    :Регистрация диплома в блокчейне;
    :Уведомление студента;
    stop
  else (нет)
    :Отправка уведомления о необходимости дополнительных подписей;
    stop
  endif
else (нет)
  :Отправка уведомления об ошибке в данных;
  stop
endif
@enduml
